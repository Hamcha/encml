#encml tool
HTML file encryption

## How to install
Just `npm install` all the dependencies and then run it

## How to use it
To get all parameters in a Yargs prompt, just call `./encml`

Example usage

    ./encml --in toEncrypt.html --out encrypted.html